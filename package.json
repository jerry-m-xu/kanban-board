{
  "name": "rest-api",
  "version": "1.0.0",
  "private": true,
  "description": "FastAPI + React REST API project",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install:frontend": "npm install --prefix frontend"
  },
  "license": "MIT"
}
